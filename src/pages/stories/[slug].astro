---
import Layout from "@/layouts/Layout.astro";
import StoryHeader from "../../components/StoryHeader.astro";
import StoryMetadata from "../../components/StoryMetadata.astro";
import ArticleBody from "../../components/ArticleBody.astro";
import RelatedItemsList from "../../components/RelatedItemsList.astro";
import {
  findStoryTranslation,
  getAllBooks,
  getAllResources,
  getAllStories,
  getAllTopics,
  getStoryRouteSlug,
  getStoriesByLanguage,
  indexEntriesBySlug,
  renderEntry,
} from "@/utils/content";
import { buildCanonicalUrl } from "@/utils/seo";

export async function getStaticPaths() {
  const stories = await getStoriesByLanguage("en");
  return stories.map((story) => ({
    params: { slug: getStoryRouteSlug(story) },
    props: { story },
  }));
}

const { story } = Astro.props;
const { Content } = await renderEntry(story);

const [allStories, allTopics, allBooks, allResources] = await Promise.all([
  getAllStories(),
  getAllTopics(),
  getAllBooks(),
  getAllResources(),
]);

const topicBySlug = indexEntriesBySlug(allTopics);
const bookBySlug = indexEntriesBySlug(allBooks);
const resourceBySlug = indexEntriesBySlug(allResources);
const storyRouteSlug = getStoryRouteSlug(story);
const bnStory = findStoryTranslation(allStories, storyRouteSlug, "bn");
const sourceBook = story.data.source_slug
  ? bookBySlug.get(story.data.source_slug)
  : undefined;

const currentUrl = new URL(Astro.url.pathname, Astro.site).toString();
const canonicalUrl = buildCanonicalUrl(Astro.url.pathname, Astro.site);
const alternateLinks = bnStory
  ? [
      { hreflang: "en", href: canonicalUrl },
      {
        hreflang: "bn",
        href: buildCanonicalUrl(`/bn/stories/${getStoryRouteSlug(bnStory)}`, Astro.site),
      },
      { hreflang: "x-default", href: canonicalUrl },
    ]
  : [];
const isBasedOn = sourceBook
  ? {
      "@type": "CreativeWork",
      name: sourceBook.data.name,
      ...(sourceBook.data.author && {
        author: {
          "@type": "Person",
          name: sourceBook.data.author,
        },
      }),
    }
  : null;

const schemaData = {
  "@context": "https://schema.org",
  "@type": "Article",
  headline: story.data.title,
  url: canonicalUrl,
  inLanguage: story.data.language,
  author: {
    "@type": "Organization",
    name: "Otibeguni",
    url: buildCanonicalUrl("/", Astro.site),
  },
  publisher: {
    "@type": "Organization",
    name: "Otibeguni",
    url: buildCanonicalUrl("/", Astro.site),
  },
  ...(isBasedOn && { isBasedOn }),
  ...(story.data.cover_image && { image: story.data.cover_image }),
};

const iconMap = {
  Deity: "fa-solid fa-burst",
  Person: "fa-solid fa-user",
  People: "fa-solid fa-users",
  Place: "fa-solid fa-location-dot",
  Site: "fa-solid fa-landmark",
  Kingdom: "fa-solid fa-chess-rook",
  Being: "fa-solid fa-ghost",
  Work: "fa-solid fa-book-open",
  Religion: "fa-solid fa-hands-praying",
  Event: "fa-solid fa-calendar-days",
  video: "fa-solid fa-video",
  website: "fa-solid fa-newspaper",
};

const relatedTopics = (story.data.topic_slugs ?? [])
  .map((slug) => topicBySlug.get(slug))
  .filter(Boolean)
  .map((topic) => ({
    name: topic.data.item,
    types: topic.data.types || [],
    url: `/topics/${topic.slug}`,
    isExternal: false,
  }));

const relatedResources = (story.data.resource_slugs ?? [])
  .map((slug) => resourceBySlug.get(slug))
  .filter(Boolean)
  .map((resource) => ({
    name: resource.data.title,
    type: resource.data.type,
    url: resource.data.url || "",
    isExternal: true,
  }));
const breadcrumbs = [
  { label: "Stories", href: "/stories" },
  { label: story.data.title },
];
---

<Layout
  title={`${story.data.title} - Stories | Otibeguni `}
  schema={schemaData}
  lang="en"
  canonicalUrl={canonicalUrl}
  alternateLinks={alternateLinks}
  breadcrumbs={breadcrumbs}
>
  <main class="py-8 md:py-12" data-pagefind-body>
    <div class="container mx-auto max-w-4xl px-4">
      <div class="bg-base-100 rounded-xl shadow-lg">
        <article class="p-6 md:p-10">
          <StoryHeader story={{ ...story.data, slug: storyRouteSlug }} />

          <StoryMetadata
            category={story.data.category}
            sourceBook={sourceBook?.data.name || story.data.source_label}
            sourceAuthor={sourceBook?.data.author}
            sourceUrl={sourceBook?.data.library_url || sourceBook?.data.url}
            translationLink={bnStory ? `/bn/stories/${getStoryRouteSlug(bnStory)}` : undefined}
            translationLabel="Read in Bengali"
          />

          <div class="divider"></div>

          <ArticleBody>
            <Content />
          </ArticleBody>

          {
            relatedTopics.length > 0 && (
              <>
                <div class="divider mt-12" />
                <RelatedItemsList
                  title="Related Topics"
                  items={relatedTopics}
                  iconMap={iconMap}
                />
              </>
            )
          }

          {
            relatedResources.length > 0 && (
              <>
                <div class="divider mt-12" />
                <RelatedItemsList
                  title="Related Resources"
                  items={relatedResources}
                  iconMap={iconMap}
                />
              </>
            )
          }
        </article>
      </div>
    </div>
  </main>
</Layout>
